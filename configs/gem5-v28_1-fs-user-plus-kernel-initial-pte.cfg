# Candidate full-system user+kernel profile for dual-PTE-boundary FST traces.
#
# Measurement PTEs and exact boundary-in-flight flags replace the heuristic
# decision; unavailable state still uses the frozen portable fallback.
config.include = gem5-v28_1-fs-user-plus-kernel.cfg

page_fault.initial_pte_state_model = true
