# Batch-local private read-service references. Mechanism tests pass, but the
# two TeaLeaf full-ROI controls show no CPI benefit. Keep explicit opt-in.
# See docs/private-read-services-phase1-20260910.md.
config.include = gem5-fs-native-kernel.cfg
core.response_private_read_services = true
