# Versioned host-throughput overlay for the maintained native full-system
# profile.  Private preview is an exact host-side acceleration: disabling it
# keeps the canonical cache/coherence/DRAM path and does not change target
# timing or PMU semantics.  Under the v28_6 materialized-UOP kernel, its
# certificate scans and domain barrier no longer amortize on the C32 pilot.
config.include = gem5-v28_6-fs-materialized-kernel.cfg

sim.interval_private_preview = false
