config.include = gem5-fs-native-kernel.cfg
# Opt-in constrained service reevaluation; see the implementation report.
# Only the canonical FCFS path is covered (including the C4 topology bypass).
core.response_shared_service_constraints = true
