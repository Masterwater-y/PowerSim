# Candidate overlay for gem5-style branch predictor state timing.
#
# The functional FST remains correct-path only. The predictor checkpoints
# recoverable direction/local history at modeled Fetch, repairs it on the
# committed branch outcome, and delays table training until modeled ordered
# retirement. No wrong-path instruction, PC, target, or oracle field is
# consumed by this candidate.
config.include = gem5-v28_2-fs-native-kernel.cfg

branch.speculative_history = true
