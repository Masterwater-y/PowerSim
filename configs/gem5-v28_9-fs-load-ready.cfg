# Ordinary-load producer timing for the captured gem5 O3/Ruby CPU.
# Resident issue -> response = 2 cycles, then 1 cycle to consumer readiness.
# Preserve the legacy atomic/store latencies and the exact two-stage kernel.
# Selected as the maintained default by explicit user decision on 2026-09-12;
# this does not imply independent held-out/trend/quiet-host gates have passed.
config.include = gem5-v28_6-fs-materialized-kernel.cfg

core.ordinary_load_latency = 3
core.load_response_to_ready = 1
