# Versioned host-throughput overlay for the maintained native full-system
# profile.  The dedicated materialized-UOP kernel compile-time specializes
# the validated sparse response feature set; it preserves the generic scalar
# path's dispatch/completion/retire and queue/event transitions exactly.
config.include = gem5-v28_5-fs-monotone-iq.cfg

core.response_materialized_uop_fast_kernel = true
