# Canonical full-system user+kernel ROI-entry page-state profile.
#
# The model is already enabled by the parent profile. This named overlay is
# retained for explicit experiment manifests and replaces the retired
# `initial-pte` candidate filename.
config.include = gem5-v28_1-fs-user-plus-kernel.cfg
