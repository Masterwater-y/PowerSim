# Versioned host-throughput overlay for the maintained native full-system
# profile.  This changes only the response-side IQ capacity data structure:
# the target-visible multiset/minimum of release cycles is identical to v28_4.
config.include = gem5-v28_4-fs-modeled-ifetch.cfg

core.response_monotone_iq_calendar = true
