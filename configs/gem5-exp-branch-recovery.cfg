# Experimental response-causal branch-recovery candidate.
#
# This is intentionally not a maintained default.  It adds only the necessary
# corrected-branch-completion -> younger committed-fetch edge in the existing
# single response pass.  Cache/coherence choices already made in the current
# checkpoint are not replayed; see docs/branch-recovery-phase1-20260907.md
# before expanding validation.
config.include = gem5-fs-native-kernel.cfg

core.response_branch_recovery = true
