# Candidate full-system user-only profile for dual-PTE-boundary FST traces.
#
# Keep this as an explicit overlay until the C4/C8 held-out gate establishes
# that guest-PTE boundary state improves CPI without workload tuning.
config.include = gem5-v28_1-fs-user.cfg

page_fault.initial_pte_state_model = true
