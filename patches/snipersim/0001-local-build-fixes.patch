diff --git a/Makefile b/Makefile
index a1c67eb..1cda74b 100644
--- a/Makefile
+++ b/Makefile
@@ -198,7 +198,7 @@ $(PIN_ROOT): $(PIN_DEP)
 $(PIN_DEP):
 	$(_MSG) '[DOWNLO] Pin 3.31-98869'
 	$(_CMD) mkdir -p $(PIN_HOME)
-	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet --show-progress $(PIN_DOWNLOAD) | tar -xf - -z --auto-compress --strip-components 1 -C $(PIN_HOME)
+	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet --show-progress $(PIN_DOWNLOAD) | tar --no-same-owner -xf - -z --auto-compress --strip-components 1 -C $(PIN_HOME)
 	$(_CMD) touch $(PIN_HOME)/.autodownloaded
 sde_kit: $(PIN_ROOT)
 else ifneq (,$(USE_PINPLAY))
@@ -208,7 +208,7 @@ $(PIN_ROOT): $(PIN_DEP)
 $(PIN_DEP):
 	$(_MSG) '[DOWNLO] Pinplay 3.11-97998'
 	$(_CMD) mkdir -p $(PIN_HOME)
-	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet --show-progress $(PIN_DOWNLOAD) | tar -xf - -j --strip-components 1 -C $(PIN_HOME)
+	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet --show-progress $(PIN_DOWNLOAD) | tar --no-same-owner -xf - -j --strip-components 1 -C $(PIN_HOME)
 	$(_CMD) touch $(PIN_HOME)/.autodownloaded
 sde_kit: $(PIN_ROOT)
 else
@@ -219,7 +219,7 @@ sde_kit: $(PIN_DEP)
 $(PIN_DEP):
 	$(_MSG) '[DOWNLO] SDE 9.44.0-2024-08-22'
 	$(_CMD) mkdir -p $(SDE_HOME)
-	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet --show-progress $(SDE_DOWNLOAD) | tar -xf - -J --strip-components 1 -C $(SDE_HOME)
+	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet --show-progress $(SDE_DOWNLOAD) | tar --no-same-owner -xf - -J --strip-components 1 -C $(SDE_HOME)
 	$(_CMD) rm -r $(SDE_HOME)/pinkit/source/tools/InstLib #It looks like it is missing source files, so it fails to compile but not necessary for sniper
 	$(_CMD) touch $(SDE_HOME)/.autodownloaded
 	$(_MSG) '[DOWNLO] pinplay-tools'
@@ -239,7 +239,7 @@ mcpat: mcpat/mcpat-1.0
 mcpat/mcpat-1.0:
 	$(_MSG) '[DOWNLO] McPAT'
 	$(_CMD) mkdir -p mcpat
-	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet "https://snipersim.org/packages/mcpat-1.0.tgz" | tar -xf - -z -C mcpat
+	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet "https://snipersim.org/packages/mcpat-1.0.tgz" | tar --no-same-owner -xf - -z -C mcpat
 endif
 linux: include/linux/perf_event.h
 include/linux/perf_event.h:
@@ -253,7 +253,7 @@ TORCH_DEP := $(SIM_ROOT)/libtorch/lib/libtorch.so
 ifeq ($(wildcard $(TORCH_DEP)),)
 torch-download:
 	$(_MSG) '[DOWNLO] LIBTORCH $(TORCH_VERSION)';
-	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet --show-progress $(TORCH_DOWNLOAD) | tar -xf - -z -C $(SIM_ROOT);
+	$(_CMD) wget -O - $(WGET_OPTIONS) --no-verbose --quiet --show-progress $(TORCH_DOWNLOAD) | tar --no-same-owner -xf - -z -C $(SIM_ROOT);
 
 torch: torch-download
 endif
diff --git a/common/Makefile.common b/common/Makefile.common
index 44c982a..a42babe 100644
--- a/common/Makefile.common
+++ b/common/Makefile.common
@@ -45,7 +45,7 @@ ifneq ($(BOOST_INCLUDE),)
 endif
 
 # Assuming python3 include dir is within the gcc search path
-PYTHON_LD_LIBS := $(shell python3-config --libs --embed)
+PYTHON_LD_LIBS := $(shell python3-config --ldflags --embed)
 LD_LIBS += -ldecoder -lsift -lxed -lrt -lz -lsqlite3 -ltorch -ltorch_cpu -lc10 $(PYTHON_LD_LIBS)
 
 LD_FLAGS += -L$(SIM_ROOT)/lib -L$(SIM_ROOT)/decoder_lib/ -L$(SIM_ROOT)/sift -L$(XED_HOME)/lib -L$(SIM_ROOT)/libtorch/lib
diff --git a/sift/Makefile b/sift/Makefile
index fc377d8..d5bff9a 100644
--- a/sift/Makefile
+++ b/sift/Makefile
@@ -3,6 +3,7 @@ OBJECTS=$(patsubst %.cc,%.o,$(SOURCES))
 TARGET=libsift.a
 
 include ../common/Makefile.common
+export SNIPER_ROOT := $(SIM_ROOT)
 
 ifeq ($(SNIPER_TARGET_ARCH),intel64)
    CXXFLAGS_ARCH=
